import styled from "styled-components";

import {tileText} from "../styles/colors";
import {tight} from "../styles/spacing";

export default styled.div`
  padding: ${tight};

  font-size: .75rem;

  color: ${tileText};
`;
